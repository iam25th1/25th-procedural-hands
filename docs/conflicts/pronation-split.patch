diff --git a/hands/src/ik.js b/hands/src/ik.js
index 447bf6d..70e8d4d 100644
--- a/hands/src/ik.js
+++ b/hands/src/ik.js
@@ -85,7 +85,7 @@ export function solveArm(skel, side, target, targetRot, pole) {
   const sn = Math.sqrt(Math.max(0, 1 - st.swing[3] * st.swing[3]));
   const axis = sn < 1e-9 ? [0, 0, 0] : [st.swing[0] / sn, st.swing[1] / sn, st.swing[2] / sn];
   const wc = { flex: -axis[0] * ang, abd: (axis[1] * ang) * wr.sideSign, twist: st.twist * wr.sideSign };
-  const offset = (wr.twistOffset || 0) * 3;
+  const offset = (t1.twistOffset || 0) + (t2.twistOffset || 0) + (wr.twistOffset || 0);
   let total = offset + wc.twist;
   total = ((total + Math.PI) % (2 * Math.PI) + 2 * Math.PI) % (2 * Math.PI) - Math.PI;
   // Continuity: of the equivalent branches, take the one nearest the last solve.
@@ -94,11 +94,12 @@ export function solveArm(skel, side, target, targetRot, pole) {
     for (const cand of [total - 2 * Math.PI, total + 2 * Math.PI]) if (Math.abs(cand - last) < Math.abs(total - last)) total = cand;
   }
   wr.lastPronation = total;
-  const twistLimit = wr.limits.twist;
-  const third = clamp(total / 3, twistLimit[0], twistLimit[1]);
-  skel.setChannels(t1, 0, 0, third);
-  skel.setChannels(t2, 0, 0, third);
-  skel.setChannels(wr, wc.flex, wc.abd * wr.abdSign, third);
+  const twistLimit = t1.limits.twist;
+  const half = clamp(total / 2, twistLimit[0], twistLimit[1]);
+  skel.setChannels(t1, 0, 0, half);
+  skel.setChannels(t2, 0, 0, half);
+  skel.setChannels(wr, wc.flex, wc.abd * wr.abdSign, 0);
+  const third = (2 * half) / 3;
   skel.update();
 
   const err = v3.dist(wr.worldPos, target);
diff --git a/hands/src/mesh.js b/hands/src/mesh.js
index a4fcdcb..a9a6103 100644
--- a/hands/src/mesh.js
+++ b/hands/src/mesh.js
@@ -237,7 +237,7 @@ export function buildArmMesh(skel, side, { lod = 'high' } = {}) {
   // pronation to full supination, where s is the share its skin weights
   // give it (0 on the elbow's and upper arm's bones, 1 on the wrist's). The
   // skinning then unwinds it exactly as the forearm supinates.
-  const TURN_SHARE = { shoulder: 0, 'upper-arm': 0, forearm: 0, 'forearm-twist-1': 1 / 3, 'forearm-twist-2': 2 / 3, wrist: 1 };
+  const TURN_SHARE = { shoulder: 0, 'upper-arm': 0, forearm: 0, 'forearm-twist-1': 1 / 2, 'forearm-twist-2': 1, wrist: 1 };
   const byBone = new Map(Object.keys(TURN_SHARE).map((n) => [bi(n), TURN_SHARE[n]]));
   const turnShare = (w) => w.reduce((acc, [bone, wt]) => acc + wt * (byBone.get(bone) ?? 1), 0) / w.reduce((acc, [, wt]) => acc + wt, 0);
   // Full pronation to full supination: half a turn about the forearm's
diff --git a/hands/src/skeleton.js b/hands/src/skeleton.js
index f5f5868..98629a5 100644
--- a/hands/src/skeleton.js
+++ b/hands/src/skeleton.js
@@ -35,10 +35,10 @@ function jointLimits(name, digit, segment) {
   if (name === 'upper-arm') return limitsFor({ flex: [0, LIMITS_DEG.shoulder.cone], twist: LIMITS_DEG.shoulder.twist });
   if (name === 'forearm') return limitsFor({ flex: LIMITS_DEG.elbow.flex });
   if (name === 'forearm-twist-1' || name === 'forearm-twist-2') {
-    return limitsFor({ twist: [LIMITS_DEG.forearm.twist[0] * THIRD, LIMITS_DEG.forearm.twist[1] * THIRD] });
+    return limitsFor({ twist: [LIMITS_DEG.forearm.twist[0] / 2, LIMITS_DEG.forearm.twist[1] / 2] });
   }
   if (name === 'wrist') {
-    return limitsFor({ flex: LIMITS_DEG.wrist.flex, abd: LIMITS_DEG.wrist.dev, twist: [LIMITS_DEG.forearm.twist[0] * THIRD, LIMITS_DEG.forearm.twist[1] * THIRD] });
+    return limitsFor({ flex: LIMITS_DEG.wrist.flex, abd: LIMITS_DEG.wrist.dev, twist: [0, 0] });
   }
   if (digit === 'thumb') {
     if (segment === 'metacarpal') return limitsFor(LIMITS_DEG.thumb.cmc);
@@ -210,8 +210,9 @@ export function buildSide(side, offset = 0) {
   // X component in this basis.
   // Pronation channels measure from the thumb up neutral; the bind pose is
   // fully pronated, a third of it on each bone of the chain.
-  const third = deg(LIMITS_DEG.forearm.bindPronation) / 3;
-  for (const name of ['forearm-twist-1', 'forearm-twist-2', 'wrist']) byName.get(name).twistOffset = third;
+  const half = deg(LIMITS_DEG.forearm.bindPronation) / 2;
+  for (const name of ['forearm-twist-1', 'forearm-twist-2']) byName.get(name).twistOffset = half;
+  byName.get('wrist').twistOffset = 0;
   // The wrist composes as a swing (flexion and deviation as one rotation
   // vector in the XY plane) followed by the pronation twist about the bone,
   // which stays continuous where an Euler split would jump.
diff --git a/scripts/hands-checks/index.js b/scripts/hands-checks/index.js
index 5b85420..9391333 100644
--- a/scripts/hands-checks/index.js
+++ b/scripts/hands-checks/index.js
@@ -14,7 +14,7 @@ import { skinChecks } from './skin.js';
 import { planChecks } from './plans.js';
 import { videoChecks } from './video.js';
 import { planningChecks } from './planning.js';
-import { skinDeformChecks } from './skin-deform.js';
+import { skinDeformChecks, pronationChecks } from './skin-deform.js';
 
 export const CHECKS = [
   ...servedChecks,
@@ -32,5 +32,6 @@ export const CHECKS = [
   ...uiChecks,
   ...skinChecks,
   ...skinDeformChecks,
+  ...pronationChecks,
   ...videoChecks,
 ];
diff --git a/scripts/hands-checks/skin-deform.js b/scripts/hands-checks/skin-deform.js
index 30dddc1..eb72332 100644
--- a/scripts/hands-checks/skin-deform.js
+++ b/scripts/hands-checks/skin-deform.js
@@ -204,6 +204,38 @@ function ringPerimeter(P, ids, axis) {
   return s;
 }
 
+// The radiocarpal joint flexes and deviates but does not pronate; the hand
+// turns with the radius, and the skin over the distal third of the forearm
+// turns with it (Kulesh et al 2015, SICOT J 1:3: skin moves least against
+// the radius in the distal third). 90 percent of the hand's turn is the
+// check's tolerance on "with it".
+export const DISTAL_TURN_MIN = 0.9;
+export const pronationChecks = [
+  {
+    name: 'skinning: the distal third of the forearm turns with the hand under 90 deg of forearm rotation (the wrist joint does not pronate)',
+    async run() {
+      const skel = new Skeleton();
+      const mesh = buildArmMesh(skel, 'right', { lod: 'high' });
+      const { rings, axis } = forearmRings(skel, mesh);
+      skel.reset();
+      const hand = skel.joint('right', 'wrist');
+      const restHand = hand.worldRot.slice();
+      const total = ['forearm-twist-1', 'forearm-twist-2', 'wrist'].reduce((a, nm) => a + skel.joint('right', nm).channels.twist, 0);
+      // 90 degrees from the bind pose to thumb up, shared as the rig shares it.
+      const shares = ['forearm-twist-1', 'forearm-twist-2', 'wrist'].map((nm) => skel.joint('right', nm).channels.twist / total);
+      ['forearm-twist-1', 'forearm-twist-2', 'wrist'].forEach((nm, k) => { const j = skel.joint('right', nm); skel.setChannels(j, 0, 0, j.channels.twist - shares[k] * (Math.PI / 2)); });
+      skel.update();
+      const handTurn = Math.abs(quat.twistAngle(quat.multiply([0, 0, 0, 1], hand.worldRot, quat.conjugate([0, 0, 0, 1], restHand)), axis));
+      const P = skinned(skel, mesh);
+      const N = skinnedNormals(skel, mesh);
+      const distal = rings.filter((r) => r.f >= 2 / 3 && r.f < 0.99);
+      const rows = distal.map((r) => ({ f: r.f, share: Math.abs(quat.twistAngle(ringRotation(mesh.positions, P, r.ids, mesh.normals, N), axis)) / handTurn }));
+      const worst = Math.min(...rows.map((x) => x.share));
+      return { pass: worst >= DISTAL_TURN_MIN, worst: worst * 100, limit: DISTAL_TURN_MIN * 100, unit: 'percent of the hand turn', note: `hand turned ${toDeg(handTurn).toFixed(0)} deg; distal third rings: ${rows.map((x) => `${(x.share * 100).toFixed(0)} percent at ${x.f.toFixed(2)}`).join(', ')}` };
+    },
+  },
+];
+
 export const skinDeformChecks = [
   {
     name: 'proportion: forearm length and wrist girth within half a standard deviation of the ANSUR II means',
