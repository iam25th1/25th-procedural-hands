// The capability matrix: one row per capability the spec asks for, with the
// named check (from npm run hands:check) and the gallery sheet that prove it.
// Rows are added in the same commit as the capability.
export const CAPABILITIES = [];
